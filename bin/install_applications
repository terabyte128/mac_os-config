#! /usr/bin/env bash

# DESCRIPTION
# Installs applications.

# EXECUTION

# install_zip_app "$ATOM_APP_URL" "$ATOM_APP_NAME"

install_zip_app "$BRIGHTNESS_APP_URL" "$BRIGHTNESS_APP_NAME"

# install_zip_app "$SPOTIFY_NOTIFICATIONS_APP_URL" "$SPOTIFY_NOTIFICATIONS_APP_NAME"
